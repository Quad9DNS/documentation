# Build dependencies for the MkDocs site. Pinned so CI builds are
# reproducible and dependency bumps arrive as reviewable Dependabot PRs.
# Versions captured from the last known-good deploy (2026-06-02).
mkdocs-material==9.7.6
mkdocs-awesome-pages-plugin==2.10.1
mkdocs-static-i18n==1.3.1
# Required by the Material social plugin for card generation.
cairosvg==2.9.0
pillow==12.2.0
